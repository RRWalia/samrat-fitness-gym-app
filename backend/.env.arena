# Tracked, non-secret fallback configuration for the Arena preview workspace.
# The OAuth *client ID* is public material (it ships to every browser); the
# client secret is NOT here and NOT needed. Local overrides go in backend/.env
# (gitignored), which takes precedence over these values.
GOOGLE_CLIENT_ID=365841864100-pmd77vokr45qdjlb78ddafptcsnl3jra.apps.googleusercontent.com
INITIAL_OWNER_EMAIL=sonarandeep@gmail.com
INITIAL_OWNER_NAME="Sonarandeep"
